{% extends 'baseFront.html.twig' %}
{% block title %}
    Making-Of
{% endblock %}

{% block body %}
    <section class="mb-5">
        <h4>Making-Of</h4>
    </section>
    <div class="container-fluid">
        <div class="row">
            {% for allMakingOf in allMakingOfs %}
                <div class="col-lg-3 mb-5 cover">
                    
                    <a href="{{ path('makingOf', {'id': allMakingOf.id}) }}">
                        <img alt="Sébastien Mao : {{ allMakingOf.album.title }}" class="mini" src="{{ vich_uploader_asset(allMakingOf.album, 'coverFile')}}">
                    </a>

                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
